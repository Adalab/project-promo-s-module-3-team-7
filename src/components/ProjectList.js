const ProjectList = () => {
    // hacer el map para pintar cada li
    {/* por ejemplo:
        const projects = list.map((eachProject, index) => {
            return <ProjectItem eachProjec={eachProject} key={index} />
        })
    */}

    return (
        <section>
            <ul></ul>
        </section>
    );
};

export default ProjectList;